#!/bin/bash

filename=$1

[-f $filename] && echo "regular file detected" || echo "not regular file"
exit  # to run code below comment it
# or variant with if
if [-f $filename] ; then 
    echo 'regular file detected
else 
    echo "not regular file"




































































































